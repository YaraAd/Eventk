import 'package:flutter/material.dart';

class ExploreEventsByDate extends StatelessWidget {
  const ExploreEventsByDate({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
